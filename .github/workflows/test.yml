name: Run Tests

on:

push:

branches:

- main

jobs:

JESTandLINT:

    runs-on: ubuntu-latest

    name: run-jest-and-lint

    steps:

    - uses: actions/checkout@v3

    - uses: bahmutov/npm-install@v1

    - run: npm run lint

    - run: npm run test
